"use client";

import type { Patient } from "@/lib/types";
import { post } from "@/lib/api";

export default function PatientDrawer({ patient, onClose, onAction }: { patient: Patient | null; onClose: () => void; onAction: () => void }) { if (!patient) return null; const activePatient = patient; async function decide(action: string) { await post(`/api/decide/${activePatient.id}/${action}${action === "override" ? "?esi=III" : ""}`); onAction(); } return <><div className="scrim on" onClick={onClose} /><aside className="drawer on"><div className="dhead"><div><div className="tag">{patient.arrival_mode} · waiting {Math.round(patient.waited)} min</div><h2>{patient.display_id} · {patient.age != null ? `age ${patient.age}` : "age unknown"}</h2></div><button onClick={onClose}>X</button></div><div className="dbody">{patient.transcript && patient.arrival_mode === "voice intake" && <div className="sec"><h4>Original account · as spoken</h4><div className="quote">{patient.transcript}</div></div>}<div className="sec"><h4>Presenting complaint · Layer 2 extraction</h4><div className="quote">{patient.complaint}</div></div><div className="sec"><h4>Vitals · {patient.vitals_present} of 6 recorded</h4><div className="vgrid">{Object.entries(patient.vitals || {}).map(([key, value]) => value != null && <div className="vc" key={key}><div className="k">{key}</div><div className="v">{value}</div></div>)}</div></div><div className="sec"><div className="gate"><div className="g">● Human decision gate</div><div className="rec">PULSE recommends<br /><b>ESI {patient.esi}</b> - {patient.confidence} confidence<br /><b>{patient.routing.pathway}</b><br /><b>{patient.routing.specialty}</b></div><div className="gbtns"><button className="acc" onClick={() => decide("accept")}>Accept</button><button className="ovr" onClick={() => decide("override")}>Override</button></div></div></div></div></aside></>; }
